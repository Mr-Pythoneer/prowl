"""Prowl — a fast, local-AI, voice-first desktop agent for macOS.

Prowl listens (hotkey or wake), understands with a fast local model
(llama3.2:3b via Ollama), and either answers instantly, runs a built-in
skill, or escalates hard/open-ended tasks to the OpenClaw agent (Claude).
"""

__version__ = "0.1.0"
